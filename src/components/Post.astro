---
interface Props {
    title: string;
    date?: string;
}
const { title, date } = Astro.props;
---

<a href="/#portfolio" class="top-0 p-4 block lg:sticky sm:w-fit"> ← Home</a>
<div class="mx-auto mt-6 mb-10 max-w-prose px-2 md:px-0 flex-1">
    <h1 class="text-3xl font-light">{title}</h1>
    {date && <h2 class="text-base mb-6">{date}</h2>}
    <div class="divide-y-2 mt-10"><slot /></div>
</div>
