---
title: A test post
date: "2025-01-15"
description: Testing the writing zone layout.
---

import Sidenote from "../../components/Sidenote.astro";

This is what the writing zone looks like. The body text sits in a comfortable reading column, with space on the right for sidenotes when needed.

## A heading

Links work like this: [external link](https://astro.build) — they get an accent-colored underline that fills to a full background on hover. That's the bnjmnmddn treatment.

Code blocks get their own treatment:

```js
const greeting = "hello from the writing zone";
console.log(greeting);
```

Inline `code` looks like this.

## Another section

<Sidenote>This is a sidenote. On wide screens it floats into the right margin, anchored to wherever it appears in the text. On mobile it collapses inline.</Sidenote>

Blockquotes take the accent color on their left border:

> The throughline is structural confidence with minimal decoration.

Images respect the column width and don't stretch beyond it.
